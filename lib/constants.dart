import 'package:flutter/material.dart';

// Access by importing constants.dart and referencing statically like:
// AppColors.primaryColor;
class AppColors {
  static const primaryColor = Color.fromARGB(255, 120, 150, 120);
  static const secondaryColor = Color.fromARGB(255, 90, 120, 90);
  static const whiteTextColor = Color.fromARGB(255, 230, 230, 230);
  static const blackTextColor = Color.fromARGB(255, 25, 25, 25);
}